# 1. Node Modules (Essential)
node_modules/

# 2. Environment Variables (Critical Security)
.env
.env.local
.env.development.local
.env.production.local

# 3. Build Output Folders (Optional but recommended)
# Since you have separate projects, you should ignore their final build folders
frontend/build
dashboard/build
backend/dist # or whatever your backend output folder is